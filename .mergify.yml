pull_request_rules:
  - name: merge only after trusted approval and visible security approval marker
    conditions:
      - approved-reviews-by = livastic-vm2-primary-20260911
      - "body ~= Security-Approval: YES"
    actions:
      merge:
        method: merge
